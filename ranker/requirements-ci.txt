# What the test suite needs, and nothing else.
#
# The heavy dependencies in requirements.txt are all imported lazily:
# sentence-transformers only when the service is asked to embed, umap-learn
# and hdbscan only by /clusters. No test loads a model or clusters anything -
# that is a deliberate property of the suite, not an accident - so CI installs
# neither them nor the ~2 GB of torch behind sentence-transformers.
#
# streamlit is absent for the same reason: the theme tests import theme.py,
# which needs plotly and nothing more. The dashboard itself is not unit tested;
# it is checked by running it.
#
# If a test ever needs one of the heavy three, install it here rather than
# reaching for requirements.txt - the split is what keeps CI under a minute.

fastapi>=0.115
pydantic>=2.9
numpy>=1.26
httpx>=0.27          # fastapi's TestClient
plotly>=5.24         # theme.py
pytest>=8.3
